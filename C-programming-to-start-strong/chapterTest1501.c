#include <stdlib.h>

enum MYENUM { TOP, BOTTOM, LEFT, RIGHT };
/*
 * 4. 게임을 개발하면서 최상위, 바닥, 왼쪽, 오른쪽에 해당하는 정보를 열거형
 * 상수로 정의하려고 합니다. 만일 열거형 상수의 이름이 `MYMOVE`이라고 할 때
 * `TOP`, `BOTTOM`, `LEFT`, `RIGHT`를 선언하려고 합니다. 어떻게 해야 하는지
 * 코드를 직접 보여주세요.
 */
int main(void) { return EXIT_SUCCESS; }
